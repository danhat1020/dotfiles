# true color support
set -g default-terminal "${TERM}"
set -sg terminal-overrides ",*:RGB"

# options
set -g base-index 1
set -g renumber-windows on
set -g status on
set -g status-interval 1
set -g mode-keys vi

# prefix key
set-option -g prefix C-space
set-option -g prefix2 none

# allow full word backspace
bind-key -n C-h send-keys C-w

# source file
unbind r
bind r source-file ~/dotfiles/.tmux.conf

unbind x
bind-key x kill-window

unbind q
bind-key q kill-session

unbind v
bind v copy-mode

bind-key -T copy-mode-vi "v" send -X begin-selection
bind-key -T copy-mode-vi "y" send -X copy-selection

bind-key k command-prompt "new-session -s '%%'"

bind-key c new-window -c "#{pane_current_path}"

setw -g mode-style "fg=#ffffff bg=#cc2949 bold double-underscore"
set -g status-interval 10


# <-|          |->
# [VISUAL STYLING]
# <-|          |->

HIGHLIGHT=#994299
DARKEN=#38323b
BRIGHT=#e6a5c7
DIMMED=#957e82
SEPARATOR=#7a6165
TEXT=#f3e9f7
BLACK=#0b090b

set -g status-left-length 100
set -g status-right-length 100
set -g status-justify absolute-centre

# left side
set -g status-left ""
set -ga status-left "#[fg=${BRIGHT},bg=default] 󰥔#[fg=${DIMMED} bold] %H:%M "
set -ga status-left "#[fg=${SEPARATOR},bg=default]⎥"
set -ga status-left "#[fg=${BRIGHT},bg=default] #[fg=${DIMMED} bold] #S "

# center
set -g status-position top
set -g status-style "fg=${DIMMED},bg=default"
set -g window-status-separator ""

set -g window-status-style "fg=${DIMMED},bg=default"
set -g window-status-format "  #I:#W  "
set -g window-status-current-style "fg=${HIGHLIGHT},bg=terminal bold"
set -g window-status-current-format "#[fg=${HIGHLIGHT},bg=terminal]#[fg=${TEXT},bg=${HIGHLIGHT} bold] #I:#W #[fg=${HIGHLIGHT},bg=terminal]"

# right side
set -g status-right ""
set -ga status-right "#[fg=${DIMMED} bold] slick#[fg=${BRIGHT},bg=default]  "
